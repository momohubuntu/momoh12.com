name: first workflow

on: push

jobs:
  build:
    run-ons: ubuntu_latest
    steps:
    - uses: actions/checkout@v3 
    - name: Run a sample github action
      run: echo "this workflow run successfully for thi particular action"
    - name: simulating error
      run: echo "error taking place": exit 1

    
